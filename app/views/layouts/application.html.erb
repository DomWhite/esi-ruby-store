<!DOCTYPE html>
<html>
  <head>
    <title>ESI Example</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- Always shows a header, even in smaller screens. -->
    <div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
      <header class="mdl-layout__header">
        <div class="mdl-layout__header-row">
          <!-- Title -->
          <span class="mdl-layout-title">Will's less than impressive emporium</span>
          <!-- Add spacer, to align navigation to the right -->
          <div class="mdl-layout-spacer"></div>
          <!-- Navigation. We hide it in small screens. -->
          <nav class="mdl-navigation">
              <%= link_to 'View all items', items_path, class: 'mdl-navigation__link' %>
          </nav>
          <nav class="mdl-navigation mdl-layout--large-screen-only">
              <%= link_to 'Add an item', new_item_path, class: 'mdl-navigation__link' %>
          </nav>
        </div>
      </header>
      <div class="mdl-layout__drawer">
        <span class="mdl-layout-title">Em - poor - ium</span>
        <nav class="mdl-navigation">
            <%= link_to 'View all items', items_path, class: 'mdl-navigation__link' %>
        </nav>
        <nav class="mdl-navigation">
            <%= link_to 'Add an item', new_item_path, class: 'mdl-navigation__link' %>
        </nav>
      </div>
      <main class="mdl-layout__content">
        <div class="page-content">
          <%= yield %>
        </div>
      </main>
    </div>

  </body>
</html>
